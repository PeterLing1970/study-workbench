# 学习工作台状态与路线图

更新日期：2026-08-31。本文从 v0.4.1 起替代早期“待实现功能清单”，避免重复开发已经上线的功能。

## 已完成

- 学生与家长双角色登录，后端强制家长只读。
- 今日任务创建、编辑、删除、完成和循环模板自动生成。
- 25 分钟番茄钟、任务时长模式、专注记录和日/周统计。
- 错题拍照录入、AI 分析降级、详情、删除、状态追踪和 1/3/7/15/30 天复习周期。
- 八科成绩录入、800 分结构、考试切换、得分率趋势和弱科诊断。
- AI 辅导对话、科目筛选、历史保存与清空。
- 家长看板与基于真实数据的周报；打开周报不产生 AI 调用，生成诊断为显式操作。
- 手机、iPad 和桌面响应式导航。

## v0.4.1 稳定性修复

- [x] 删除周报中的演示数字回填，空数据保持真实 0 值。
- [x] 删除“七月期末摸底”和“八月阶段测验”示例成绩，并停止自动生成。
- [x] 周报 GET 改为无副作用读取，AI 生成改为 POST。
- [x] 修正首次错题复习应安排在 1 天后的周期偏移。
- [x] 启用旧数据库幂等字段迁移。
- [x] 增加周报、任务模板和复习周期回归测试。
- [x] AI 回答安全渲染 Markdown，增加对话动态播报。
- [x] 成绩趋势默认聚焦最近考试的三门低分科目。
- [x] 弹窗增加焦点锁定、Esc 关闭和焦点恢复。
- [x] 固定前端依赖版本，统一版本号为 v0.4.1。
- [x] 增加生产安全响应头、同源写请求保护和恢复脚本。

## 下一阶段候选

### P1：真实图片理解

接入 MiniMax Token Plan 支持的官方 MCP 图片理解路径；在完成前继续明确标记演示分析，不能把文本模型降级结果伪装成图片识别。

### P1：数据质量与可观测性

- AI 调用增加每日限额、耗时和失败率统计。
- 增加数据库迁移工具与迁移版本表，替换长期依赖启动时 `ALTER TABLE` 的方式。
- 为周报增加数据覆盖说明，例如“本周有记录的天数”。

### P2：学习闭环增强

- 从错题详情一键带入 AI 辅导。
- 成绩趋势支持自选 1–3 科对比和无障碍数据表。
- 支持错题导出 PDF、每周目标与完成回顾。

### P2：家庭扩展

当前模型按单个学生家庭使用设计。如需多个孩子，必须先为任务、错题、成绩、专注和 AI 对话增加 `student_id` 数据归属，不能仅增加更多登录账号。
